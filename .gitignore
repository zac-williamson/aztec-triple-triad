node_modules/
dist/
build/
.env
.env.local
OPEN_API_KEY.txt
Github_PAT.txt
*.pem
.claude/
.DS_Store
*.log
slack-webhook.txt
# Card art is committed as .webp only (see scripts/compress-card-assets.ts).
# generate-card-art.ts emits PNG intermediates — never commit them.
packages/frontend/public/cards/**/*.png

# Deploy keys
aztec-triple-triad-deploy-key
aztec-triple-triad-deploy-key.pub

# GLIBC compatibility shim for bb binary (Amazon Linux 2023)
bb_patched
glibc_shim.so
bb_wrapper.sh

# Redis persistence files — should not be committed
dump.rdb
*.rdb

# Contract compilation backup artifacts
packages/contracts/target/*.bak

# Test coverage output (vitest --coverage)
coverage/

# Playwright run output
test-results/

# Per-worktree lane pointer (untracked, see docs/plan/MASTER_PLAN.md)
/LANE.md

# Vercel token — never commit
vercel*.txt
vercel.txt
